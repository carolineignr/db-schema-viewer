import React from 'react';
import styles from './Footer.module.scss';

export const Footer = (): any => (
  <footer className={styles.footer}>
    <span>
      <small>Ana Caroline I. R. / 2022.</small>
    </span>
  </footer>
);
